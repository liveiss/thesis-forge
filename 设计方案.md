# 论文工坊 — 产品设计方案 v0.1

> AI 辅助论文修改工具 · 独立项目
> 整理：天元 | 日期：2026-07-29

---

## 一、产品定位

### 一句话
AI 辅助论文修改工具，帮毕业生润色、降 AI 率、查逻辑、调格式。

### 核心原则
- ✅ **辅助修改**，不是代写
- ✅ 帮学生把"写得不太行"的论文改好
- ❌ 不从零生成论文

### 目标用户
- 本科/硕士毕业生（主力）
- 博士生（次要）
- 留学生（英文论文，远期）

### 市场窗口
- **2-5月**：毕业季高峰，核心变现期
- **9-11月**：开题季，次要窗口
- 必须在 **2月前** 上线可商用版本

---

## 二、核心功能

### 2.1 三大功能模块

#### 🔍 论文检测器

| 维度 | 检测内容 |
|------|----------|
| **AI痕迹检测** | 套路句式、AI标志性修辞、机械表达、过度对称结构、"不是…而是…"等 |
| **一致性检测** | 前后结论矛盾、数据引用不一致、定义前后冲突 |
| **论证逻辑检测** | 因果链断裂、论据不支持论点、循环论证、概念偷换 |
| **格式规范检测** | 引用格式（APA/MLA/GB/T）、图表编号连续性、标题层级 |
| **学术规范检测** | 口语化表达、主观断言、缺少出处、过度绝对化 |

#### ✏️ 智能润色
- 一键润色段落（保留原意，优化表达）
- 学术化改写（口语→书面、主观→客观）
- 降 AI 率改写（打散 AI 句式，增加自然度）
- 长句拆分 / 短句合并

#### 📊 降重预检
- 段落级相似度估算
- 高重复段落标红 + 改写建议
- 引用 vs 抄袭识别（有引用标注的段落跳过）

### 2.2 工作流程

```
上传论文 → 自动拆分章节 → 三维度检测 → 问题报告 → 逐项修改 → 重新检测 → 导出
```

1. **导入**：粘贴全文 或 上传 .docx
2. **自动拆分**：按标题层级识别章节结构
3. **检测**：一键全量检测，出报告
4. **修改**：逐条问题点击修正，或按章节批量修正
5. **复检**：修正后重新检测对应维度
6. **导出**：导出 .docx + 修改记录报告

---

## 三、技术方案

### 3.1 技术栈
- **前端**：Next.js + TailwindCSS（与灵境同栈，复用组件）
- **后端**：Next.js API Routes
- **AI**：OpenAI 兼容 API（DeepSeek/GPT/GLM 均可）
- **文档处理**：mammoth（docx 解析）+ docx（导出）
- **部署**：Vercel / 自建

### 3.2 复用灵境的模块
- 检测器框架（三维度 + 自动修正闭环）
- IssueCard / 高亮 / 批量修正 UI
- fuzzyFindRange / 段落定位
- AI-A/AI-B 双AI架构（AI-A → 学术审稿指挥官）

### 3.3 数据结构

```typescript
interface ThesisProject {
  title: string;
  type: 'bachelor' | 'master' | 'phd' | 'journal';
  major: string;
  citationStyle: 'APA' | 'MLA' | 'GBT' | 'Chicago';
  wordLimit: number;
  sections: ThesisSection[];
  references: Reference[];
  argumentChain: ArgNode[];
}

interface ThesisSection {
  id: string;
  title: string;
  type: 'abstract' | 'intro' | 'lit_review' | 'method' | 'result' | 'discussion' | 'conclusion' | 'reference';
  content: string;
  wordCount: number;
}

interface ThesisDiagnostic {
  id: string;
  sectionId: string;
  timestamp: number;
  aiTrace: { score: number; issues: ThesisIssue[] };
  consistency: { score: number; issues: ThesisIssue[] };
  logic: { score: number; issues: ThesisIssue[] };
  format: { score: number; issues: ThesisIssue[] };
  academic: { score: number; issues: ThesisIssue[] };
  overallScore: number;
}

interface ThesisIssue {
  severity: 'high' | 'medium' | 'low';
  category: string;
  evidence: string;
  suggestion: string;
  startSnippet: string;
}
```

### 3.4 API 消耗估算

| 操作 | API 调用 | 耗时 |
|------|----------|------|
| 全文检测 | 1 次（合并 prompt） | ~30s |
| 单段落润色 | 1 次 | ~10s |
| 章节批量修正 | 1-3 次 | ~30s |
| 重新检测（单维度） | 1 次 | ~15s |
| 完整流程 | ~5-8 次 | ~3-5min |

每篇成本约 0.1-0.3 元。

---

## 四、变现设计

### 4.1 定价

| 档位 | 价格 | 包含 |
|------|------|------|
| 免费体验 | 0 | 检测 1 次（只出报告不修改） |
| 单篇基础 | 19.9 | 检测 + 润色 + 降AI率 + 导出 |
| 单篇深度 | 39.9 | 基础 + 逻辑重构 + 格式校验 + 引用检查 |
| 毕业季通行证 | 99 | 无限次检测+修改 |

### 4.2 增值服务
- 英文论文润色（+50%）
- 查重预检（接入第三方 API）
- 导师视角评审报告

---

## 五、推广策略

### 5.1 冷启动
- 高校社群：贴吧、QQ群、微信群
- 小红书/抖音：展示"AI 写的论文被检测出来 vs 修改后通过"
- 免费检测引流，修改付费

### 5.2 裂变
- 邀请同学 → 双方各得 1 次免费深度修改
- 分享修改效果 → 领优惠券

### 5.3 时间节点
- **1月**：内测
- **2月**：正式上线
- **3-4月**：高峰推广
- **5月**：最后冲刺

---

## 六、MVP 优先级

### P0（1月上线）
- [ ] Next.js 项目搭建
- [ ] docx 导入 + 章节自动拆分
- [ ] 三维度检测（AI痕迹/一致性/论证逻辑）
- [ ] 检测报告 + 问题高亮
- [ ] 单段落润色修改
- [ ] 批量修正 + 自动复检
- [ ] docx 导出

### P1（2月上线）
- [ ] 降重预检
- [ ] 引用格式校验
- [ ] 格式规范检测
- [ ] 支付接入

### P2（3月上线）
- [ ] 学科定制 prompt
- [ ] 导师视角评审报告
- [ ] 英文论文支持
- [ ] 裂变功能

---

## 七、风险与对策

| 风险 | 对策 |
|------|------|
| 学术诚信争议 | 定位"辅助修改"，不做代写；导出修改记录可追溯 |
| AI 检测误判 | 检测结果仅供参考，不做"保证通过"承诺 |
| 查重接口成本 | 先不自带查重，引导用户用学校系统查重后回来改 |
| 竞品 | 差异化：更便宜、中文场景更懂、降AI率是核心卖点 |

---

## 八、待确认事项

- [ ] 品牌名/域名
- [ ] 是否需要用户注册系统
- [ ] 支付方式
- [ ] 是否接入第三方查重 API

---

*本文档持续更新中。天元维护。*
